import 'package:flutter/material.dart';

class BaanaColors {
  // Primaire & Accents
  static const Color primary = Color(0xFF10B981); // Vert Émeraude
  static const Color accent = Color(0xFFF0A050); // Orange Baana
  static const Color cta = Color(0xFFF97316); // Orange CTA fort

  // Neutres teintés émeraude (Zéro gris/noir pur)
  static const Color background = Color(0xFFF7FAF8); // Blanc teinté
  static const Color inputBackground = Color(0xFFEDF2EF); // Gris teinté clair
  static const Color border = Color(0xFFD4DDD8); // Gris teinté moyen
  static const Color textSecondary = Color(0xFF6B7D75); // Gris teinté foncé
  static const Color textPrimary = Color(0xFF2C3E36); // Quasi-noir teinté

  // Couleurs fonctionnelles
  static const Color error = Color(0xFFE05252); // Rouge teinté
  static const Color info = Color(0xFF3B9EC4); // Bleu teinté
  static const Color success = Color(0xFF10B981); // Vert (identique à primary)
  static const Color warning = Color(0xFFF0A050); // Orange (identique à accent)

  // Fonds très clairs pour les badges
  static const Color paleGreen = Color(0xFFE6F8F0); // Vert pâle
  static const Color paleOrange = Color(0xFFFDF0E6); // Orange pâle
}
